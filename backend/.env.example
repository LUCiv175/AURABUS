MONGO_USER =
MONGO_PASSWORD =

API_URL =
API_USER =
API_PASSWORD =

JWT_SECRET =
REFRESH_SECRET =
GOOGLE_AUTH_CLIENT_ID = 

NODE_ENV = development
# Public URL advertised in the Swagger docs; unset means the dev server.
PUBLIC_API_URL=

PREDICTION_URL=
# Shared secret so the API bypasses the AI service's public rate limit.
INTERNAL_API_KEY=

MONGO_URI=
# Database the AI trainer reads. Required when MONGO_URI has no /dbname path.
# Leave empty to use the database in MONGO_URI (a path-less URI means `test`).
MONGO_DB=

REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=